---
bump: minor
category: Changes
---

Add an AI-authorship disclosure rule to `org/writing-style`: an agent-generated PR body ends with a disclosure line naming the tool that wrote it, because the PR opens under the user's account and a reviewer would otherwise assume a person wrote it. `org/durable-writing` and its worked examples now treat that line as the one part of a body never trimmed.
