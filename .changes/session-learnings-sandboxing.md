---
bump: minor
category: Features
---

Add the `moduleloader-sandboxing` knowledge skill (world model, same-loader invariant, environment detection, cross-world React debugging) and the org `instrumented-debugging` process skill. Extend `build-and-toolchain` with the two-Flipbooks dev-place topology and three build-hygiene traps, and give `failure-archaeology` an extendable `incidents/` directory seeded with the sandboxed-Storyteller React crash case study.
