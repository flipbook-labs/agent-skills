return {
	package = {
		name = "AgentSkills",
		version = "0.5.0",
		-- Pure skill library: markdown SKILL.md files under src/, no runtime dependencies.
		-- Consumers add this package and read skills from the installed store path; see README.md.
		dependencies = {},
	},
}
