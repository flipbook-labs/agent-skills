name: Changelog

on:
  pull_request:

jobs:
  require-entry:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7

      - uses: flipbook-labs/changewrite@v0.5.0
        with:
          require-entry: "true"
